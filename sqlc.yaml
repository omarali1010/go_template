version: "1"
packages:
  - name: "db"
    path: "internal/db"      # where generated Go code goes
    queries: "./queries/*"    # folder where your SQL query files live
    schema: "./schema.sql"   # your schema file
    engine: "postgresql"
    emit_json_tags: true
    emit_prepared_queries: false
    emit_interface: false

